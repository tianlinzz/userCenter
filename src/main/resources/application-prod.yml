# prod，生产环境配置文件
spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: '自己的远程数据库地址，也最好不要暴露' # 配置生产环境数据库，这里使用的是本地数据库
    username: root
    password: 'xxxxxx' # 配置生产环境数据库密码，上线最好不要暴露密码
  session:
    timeout: 86400  # session过期时间, 两天